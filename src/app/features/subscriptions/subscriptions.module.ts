import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * M09 - Suscripciones.
 * Las páginas son standalone y se cargan por ruta.
 */
@NgModule({
  imports: [CommonModule]
})
export class SubscriptionsModule {}
