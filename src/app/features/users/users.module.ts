import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * M02 - Usuarios.
 * Las paginas son standalone y se cargan por ruta. Este modulo queda como
 * ancla documental del bounded context. Ver features/MODULES.md.
 */
@NgModule({
  imports: [CommonModule]
})
export class UsersModule {}
